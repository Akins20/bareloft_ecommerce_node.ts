<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f8f9fa;
        }
        
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .header {
            background: linear-gradient(135deg, #2D5CFF 0%, #1E40AF 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 28px;
            margin-bottom: 8px;
            font-weight: 700;
        }
        
        .header p {
            font-size: 16px;
            opacity: 0.9;
        }
        
        .content {
            padding: 30px 20px;
        }
        
        .section {
            margin-bottom: 30px;
        }
        
        .section h2 {
            color: #333;
            margin-bottom: 15px;
            font-size: 24px;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 8px;
        }
        
        .section h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 20px;
        }
        
        .highlight-box {
            background-color: #e3f2fd;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #2196f3;
        }
        
        .promo-code {
            background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        
        .promo-code-text {
            font-size: 24px;
            font-weight: bold;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }
        
        .promo-description {
            font-size: 14px;
            opacity: 0.9;
        }
        
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        
        .product-item {
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            overflow: hidden;
            transition: box-shadow 0.2s ease;
        }
        
        .product-item:hover {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        
        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        
        .product-info {
            padding: 15px;
        }
        
        .product-name {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 8px;
            color: #333;
        }
        
        .product-price {
            font-size: 18px;
            font-weight: bold;
            color: #2196f3;
            margin-bottom: 10px;
        }
        
        .original-price {
            text-decoration: line-through;
            color: #666;
            font-size: 14px;
            margin-right: 8px;
        }
        
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #2D5CFF 0%, #1E40AF 100%);
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            transition: transform 0.2s ease;
            width: 100%;
            box-sizing: border-box;
        }
        
        .cta-button:hover {
            transform: translateY(-2px);
        }
        
        .cta-button.secondary {
            background: white;
            color: #2D5CFF;
            border: 2px solid #2D5CFF;
        }
        
        .cta-section {
            text-align: center;
            margin: 30px 0;
        }
        
        .social-links {
            text-align: center;
            margin: 20px 0;
        }
        
        .social-links a {
            display: inline-block;
            margin: 0 10px;
            padding: 10px;
            background-color: #f0f0f0;
            border-radius: 50%;
            text-decoration: none;
            transition: background-color 0.2s ease;
        }
        
        .social-links a:hover {
            background-color: #2D5CFF;
            color: white;
        }
        
        .footer {
            background-color: #333;
            color: white;
            padding: 25px 20px;
            text-align: center;
        }
        
        .footer p {
            margin-bottom: 8px;
        }
        
        .footer a {
            color: #2D5CFF;
            text-decoration: none;
        }
        
        .unsubscribe-link {
            font-size: 12px;
            opacity: 0.7;
            margin-top: 15px;
        }
        
        @media (max-width: 600px) {
            .email-container {
                margin: 0 10px;
            }
            
            .content {
                padding: 20px 15px;
            }
            
            .product-grid {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 24px;
            }
            
            .promo-code-text {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <h1>{{title}}</h1>
            {{#if subtitle}}<p>{{subtitle}}</p>{{/if}}
        </div>
        
        <!-- Content -->
        <div class="content">
            <!-- Main Message -->
            {{#if message}}
            <div class="section">
                <p style="font-size: 16px; line-height: 1.7;">{{message}}</p>
            </div>
            {{/if}}
            
            <!-- Promo Code -->
            {{#if promoCode}}
            <div class="promo-code">
                <div class="promo-code-text">{{promoCode.code}}</div>
                <div class="promo-description">{{promoCode.description}}</div>
                {{#if promoCode.validUntil}}
                <div style="font-size: 12px; margin-top: 10px; opacity: 0.8;">
                    Valid until {{promoCode.validUntil}}
                </div>
                {{/if}}
            </div>
            {{/if}}
            
            <!-- Highlight Box -->
            {{#if highlight}}
            <div class="highlight-box">
                <h3 style="color: #1976d2; margin-bottom: 10px;">{{highlight.title}}</h3>
                <p>{{highlight.content}}</p>
            </div>
            {{/if}}
            
            <!-- Featured Products -->
            {{#if products}}
            <div class="section">
                <h2>{{productsTitle}}</h2>
                <div class="product-grid">
                    {{#each products}}
                    <div class="product-item">
                        {{#if image}}<img src="{{image}}" alt="{{name}}" class="product-image">{{/if}}
                        <div class="product-info">
                            <div class="product-name">{{name}}</div>
                            <div class="product-price">
                                {{#if originalPrice}}<span class="original-price">₦{{originalPrice}}</span>{{/if}}
                                ₦{{price}}
                            </div>
                            <a href="{{url}}" class="cta-button" style="font-size: 14px; padding: 10px 20px;">
                                View Product
                            </a>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}
            
            <!-- Call to Action -->
            {{#if cta}}
            <div class="cta-section">
                <a href="{{cta.url}}" class="cta-button">{{cta.text}}</a>
                {{#if cta.secondary}}
                <div style="margin-top: 15px;">
                    <a href="{{cta.secondary.url}}" class="cta-button secondary">{{cta.secondary.text}}</a>
                </div>
                {{/if}}
            </div>
            {{/if}}
            
            <!-- Social Links -->
            {{#if socialLinks}}
            <div class="social-links">
                <p style="margin-bottom: 15px; font-weight: 600;">Follow us for more updates:</p>
                {{#if socialLinks.facebook}}<a href="{{socialLinks.facebook}}">📘</a>{{/if}}
                {{#if socialLinks.twitter}}<a href="{{socialLinks.twitter}}">🐦</a>{{/if}}
                {{#if socialLinks.instagram}}<a href="{{socialLinks.instagram}}">📷</a>{{/if}}
                {{#if socialLinks.whatsapp}}<a href="{{socialLinks.whatsapp}}">💬</a>{{/if}}
            </div>
            {{/if}}
            
            <!-- Additional Content -->
            {{#if additionalContent}}
            <div class="section">
                {{{additionalContent}}}
            </div>
            {{/if}}
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p><strong>Bareloft</strong> - Your trusted e-commerce partner</p>
            <p>Lagos, Nigeria | <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
            
            {{#if unsubscribeUrl}}
            <div class="unsubscribe-link">
                <a href="{{unsubscribeUrl}}">Unsubscribe from marketing emails</a>
            </div>
            {{/if}}
            
            <p style="font-size: 12px; opacity: 0.8; margin-top: 15px;">
                This email was sent to {{customerEmail}}. 
                © 2024 Bareloft. All rights reserved.
            </p>
        </div>
    </div>
</body>
</html>